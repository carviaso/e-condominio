@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Desconectar", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Conectar", "LogOn", "Account") ]
}
